# 完整投票构建器

## Goal

编辑器里用构建器写 `[poll]`：类型、选项、可见性、图表、定时关闭，并能回编已有投票。

## Acceptance Criteria

- [x] 单选 / 多选 / 数字评分。
- [x] 结果可见性、公开投票人、柱状/饼图、自动关闭时间。
- [x] 选中已有 `[poll]` 可打开构建器回填。
- [x] 插入的是 BBCode，帖内现有投票渲染仍工作。

## Out Of Scope

- ranked_choice。
