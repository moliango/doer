# 实施计划

1. 扩展草稿数据解码，覆盖云端分类/标签字段变体。
2. 移除 `DraftsViewController` 的本地草稿 section 和分支。
3. 修改新主题、回复、私信编辑器：只保存云端草稿；加入保存草稿按钮；舍弃时删除云端草稿并阻止离开回调重存。
4. 增加/更新草稿模型与路由测试。
5. 运行 Swift 测试或可行的 Xcode 构建，并检查 `git diff`。
