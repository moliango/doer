# Implement

1. Policies + tests: history, quote cycle, block range, list convert, formatting tools, poll focused replace.
2. `ExperimentalComposerView`: history, block range, reorder, image/quote actions, formatting, restore focus, key/shake undo.
3. `ComposerToolPanelView` selected tints; coordinator poll uses focused block.
4. Reply / NewTopic / PM: source toggle without abandon; preview restores focus; selection updates tools; shake undo.
5. Spec line in `.trellis/spec/frontend/composer.md`.
6. `xcodebuild` DoerTests generic iOS Simulator, no boot.

Rollback: revert this task's files; flag stays default off.
